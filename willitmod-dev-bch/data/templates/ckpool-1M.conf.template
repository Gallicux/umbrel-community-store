{
  "btcd": [
    {
      "url": "bchn:${BCH_RPC_PORT}",
      "auth": "${RPC_USER}",
      "pass": "${RPC_PASSWORD}",
      "notify": true
    }
  ],
  "btcaddress": "${PAYOUT_ADDRESS}",
  "btcsig": "/mined by WillItMod on 5tratumOS/",
  "mindiff": 1000000,
  "startdiff": 1000000,
  "maxdiff": 0,
  "logdir": "/www",
  "webdir": "/www/pool",
  "userdir": "/www/users",
  "zmqblock": "tcp://bchn:${BCH_ZMQ_HASHBLOCK_PORT}"
}
